$(function () {
    $("#start").html("Go!");
});

/*classic JavaScript code looked like:

var el = document.getElementById("start");
el.innerHTML = "Go!";
#####################
SYNTAX:
The basic syntax of jQuery is
$("selector").action()
-The $ accesses jQuery;
-The (selector) finds the HTML elements;
-The action() is then performed on the element(s).
*
jQuery uses the CSS syntax.
*/